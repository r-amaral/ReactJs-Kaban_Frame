import React from "react";

import { AuthenticateTemplate } from "../../Components";

import * as S from "./styles";

const Register = () => {
  return (
    <AuthenticateTemplate>
      <input type="text" />
    </AuthenticateTemplate>
  );
};

export default Register;
